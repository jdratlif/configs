---
services:
  devc:
    build: .
    command: sleep infinity
    environment:
      ANSIBLE_CONFIG: /workspace/.devcontainer/ansible.cfg
    network_mode: host
    volumes:
      - ..:/workspace:cached,z
      - ${HOME}/private:/private:z
      - ${HOME}/.ssh:/root/.ssh:z
