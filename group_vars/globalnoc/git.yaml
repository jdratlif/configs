---
gitconfig:
  name: John Ratliff
  email: "{{ ansible_user_id }}@globalnoc.iu.edu"
  credentials:
    - name: https://github.grnoc.iu.edu
      username: "{{ ansible_user_id }}"
      helper: cache --timeout 14400
    - name: https://github.nwave.noaa.gov
      username: "{{ ansible_user_id }}"
      helper: cache --timeout 14400
